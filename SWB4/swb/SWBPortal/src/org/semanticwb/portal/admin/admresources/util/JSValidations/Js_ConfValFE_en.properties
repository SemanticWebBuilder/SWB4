# Sample ResourceBundle properties file

msgFormatRequired=Value defined does not correspond with the valid characters:

msgLengthRequired=, or it does not fulfill the minimum amount of characters:

msgNonvalid=This field contains nonvalid characters