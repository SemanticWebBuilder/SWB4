<?xml version="1.0" encoding="utf-8"?>
<admresource xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <xslfile>admresource.xslt</xslfile>
    <form name="frmAddNew" action="true" method="post" enctype="multipart/form-data" >
    <admdbconnmgr defconn="true"/>
    <fieldset>
        <input type="text" id="cluster" name="cluster" label="Subtipo de Banner:" size="50" required="true" promptMessage="Nombre del Subtipo de Banners que se incluyen en el carrusel" />
        <input type="text" id="width"  name="width" label="Ancho de las imágenes:" value="143" maxlength="4" regExp="\d{1,4}" invalidMessage="Ancho inválido" promptMessage="Ancho del banner, considerada en pixeles" />
        <input type="text" id="height" name="height" label="Alto de las imágenes:" value="208" maxlength="4" regExp="\d{1,4}" invalidMessage="Altura inválida" promptMessage="Altura del banner, considerada en pixeles" />
    </fieldset>
    <fieldset>
        <statictext name="cmd">
        <![CDATA[
        <button id="btnSave" dojoType="dijit.form.Button">Guardar</button>
        <button id="btnReset" dojoType="dijit.form.Button">Reestablecer</button>
        ]]>
        </statictext>
    </fieldset>
    </form>

    <script type="text/javascript">
        <![CDATA[
        dojo.require("dijit.form.Button");
        dojo.require("dijit.form.ValidationTextBox");

        dojo.addOnLoad(
            function() {
                dojo.connect(dijit.byId("btnSave"), "onClick", function(){ if(jsValida(dojo.byId("frmAddNew"))){ dojo.byId("frmAddNew").submit(); } } );
                dojo.connect(dijit.byId("btnReset"), "onClick", function(){ dojo.byId("frmAddNew").reset(); } );
            }
        );

        function jsValida(frm) {
            var cluster = dojo.byId('cluster').value;
            if( !cluster ) {
                alert('Especifique el Subtipo de Banner');
                dojo.byId('cluster').focus();
                return false;
            }
            return true;
        }
        ]]>
    </script>
</admresource>