#WebBuilder Resource Property File



findwhat=Find::

replaceWith=Replace with:

matchCase=Case sensitive match

WholeWords=Match whole words only

find=Find

replace=Replace

replaceAll=Replace all

close=Close
